# SEO Analyzer Environment Variables

# OpenAI API Key
# Required for AI-powered recommendations
# If not provided or invalid, the app will use fallback recommendations
OPENAI_API_KEY=sk-your-openai-api-key-here

# Enable/Disable GPT Recommendations
# Set to "false" to completely disable OpenAI API calls and use only fallback recommendations
# Default: true
USE_GPT_RECOMMENDATIONS=true

# Enabled GPT Checks
# Comma-separated list of checks that should use GPT for recommendations
# Empty value means no checks will use GPT
# Default: "Keyphrase in Title,Keyphrase in Meta Description,Keyphrase in Introduction,Keyphrase in H1 Heading,Keyphrase in H2 Headings"
ENABLED_GPT_CHECKS=Keyphrase in Title,Keyphrase in Meta Description,Keyphrase in Introduction,Keyphrase in H1 Heading,Keyphrase in H2 Headings

# Port for the server (optional)
PORT=5000
